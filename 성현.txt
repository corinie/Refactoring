백준사이트 1259번 팰린드롬수 / 2018-05-29

1~9999까지 숫자를 while문으로 받고 이 숫자를 문자열로 받아서 String 값의 length만큼 for문을 돌려서

한글자씩 잘라서 배열에 넣어준다. 배열에 배열에 담긴 length의 값의 /2 해준 값에 소수점을 버림해준다.

그리고 그 숫자를 기준으로 작은 값끼리 문자열로 더해주고 큰값끼리 더해준다

예) Strng[] str = [1, 2, 3, 4, 5] 중간 값을 기준으로 str[0] + str[1] 해주고 str[4] + str[3] 해준다.

이후 두 문자열을 비교 후 true면 yes, false면 no값을 프린트 해준다. while문이 정상적으로 종료되면

print문을 사용해서 0 값을 출력해준다.
 